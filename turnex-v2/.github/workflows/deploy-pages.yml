name: (disabled) Legacy Deploy to GitHub Pages

on:
  # Disabled by design to avoid duplicate deployments from nested folder
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled; deploy only from repository root."
