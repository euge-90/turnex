# Pasos después de copiar DATABASE_URL

1. Editar server/.env
2. Ejecutar estos comandos en orden:

cd server
npx prisma generate
npx prisma migrate deploy

# Si todo sale bien, verás:
# "All migrations have been successfully applied"
